"""Streamlit dashboard (Phase 6 — Serving) over the personal_finance.api FastAPI layer.

Run via ``pf dashboard`` (wraps ``streamlit run Overview.py``), not imported
directly — Streamlit re-executes each page script top to bottom per interaction.
"""
